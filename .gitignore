# Secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# OverTheWire wargame scratch — holds level passwords + spoilers.
# NEVER commit (OverTheWire's own "don't post passwords/spoilers" rule + OPSEC).
overthewire/

# CTF solution scratch (CyLab/picoCTF etc.) — kept LOCAL, not published.
# Keeps raw solutions off the public repo and keeps the cold-redo honest
# (the answer script shouldn't be sitting in the working tree at redo time).
# The redo-tracking log lives at external/cylab-notes.md and IS committed.
ctf-work/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Lab artifacts (kept local, not for portfolio)
lab/
captures/
*.pcap
*.pcapng

# Build / install
build/
dist/
node_modules/

# Logs that may contain sensitive data
*.log
logs/

# Exception: synthetic challenge data is safe to commit (whitelisted)
# - Generated by the tutor for portfolio challenges
# - Uses RFC-reserved documentation IP ranges and obviously fake usernames
# - Required for write-up reproducibility
!labs/**/*.log
